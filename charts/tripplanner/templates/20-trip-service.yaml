apiVersion: v1
kind: Service
metadata:
  name: trip-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: trip-service
  ports:
  - port: {{ .Values.trip.port }}
    targetPort: {{ .Values.trip.port }}
  type: ClusterIP