apiVersion: v1
kind: Service
metadata:
  name: trip-db
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: trip-db
  ports:
  - port: {{ .Values.tripDb.port }}
    targetPort: {{ .Values.tripDb.port }}
  type: ClusterIP
  